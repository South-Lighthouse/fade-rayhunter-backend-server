# Rate limit zones — defined at http level, included before server blocks.
# telemetry: 60 requests/minute per IP (1 per second average, burst of 10)
limit_req_zone $binary_remote_addr zone=telemetry:10m rate=60r/m;
